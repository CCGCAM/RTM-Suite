# remove outliers

remove outliers

## Usage

``` r
filter_outliers(df = NULL, input = NULL)
```

## Arguments

- df:

  a dataframe with the column to remove outliers

- input:

  string, name of the variable

## Value

a vector
