# Normalize

Normalize

## Usage

``` r
normalize(x, low = 0, high = 1)
```

## Arguments

- x:

  numeric vector to rescale.

- low:

  numeric. Lower bound of the target range. Default 0.

- high:

  numeric. Upper bound of the target range. Default 1.

## Value

normalize values
