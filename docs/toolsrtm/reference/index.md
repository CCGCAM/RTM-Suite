# Package index

## All functions

- [`calctav()`](calctav.md) : Computation of transmissivity of a
  dielectric plane surface, averaged over all directions of incidence
  and over all polarizations.

- [`calc_fluspect_bcar()`](calc_fluspect_bcar.md) : calc_fluspect_bcar
  function

- [`campbell()`](campbell.md) : Campbell function

- [`color.gradient()`](color.gradient.md) : Gradient scale Map a numeric
  vector to a color gradient

- [`Compute_BRF()`](Compute_BRF.md) : Computes bidirectional reflectance
  factor based on outputs from PRO-4SAIL and sun position

- [`compute_stats()`](compute_stats.md) : Compute statistical metrics
  for model performance

- [`ConservativeScattering()`](ConservativeScattering.md) : computes
  conservative scattering conditions

- [`correlatedValue()`](correlatedValue.md) : Generate a variable
  correlated with an existing one

- [`COST_4Fluspect()`](COST_4Fluspect.md) : Cost function for Fluspect
  parameter fitting

- [`dcum()`](dcum.md) : dcum function

- [`define_bands()`](define_bands.md) : define spectral region

- [`define_constants()`](define_constants.md) : Define constant for
  FLuxPesp

- [`dladgen()`](dladgen.md) : Computes the leaf angle distribution
  function value (freq)

- [`ephoton()`](ephoton.md) : Function for calculating the energy of a
  single photon given its wavelength.

- [`exp1_base()`](exp1_base.md) : Compute Exponential Integral E1(x)

- [`extract_dates_from_tiff_files()`](extract_dates_from_tiff_files.md)
  : Extract acquisition dates from a set of TIFF file names

- [`filter_outliers()`](filter_outliers.md) : remove outliers

- [`foursail.inf()`](foursail.inf.md) :

  `foursail.inf` The foursail (or 4SAIL) radiative transfer model is a
  RT model uses for simulating bidirectional reflectance distribution
  functions within vegetation canopies

- [`foursail.inform()`](foursail.inform.md) : Performs PROSAIL
  simulation based on a set of combinations of input parameters for
  estimating understorey reflectance

- [`foursail()`](foursail.md) : fourSAIL model coupled with several leaf
  models

- [`foursail2()`](foursail2.md) : Performs fourSAIL2 + PROSPECT
  simulation based on a set of combinations of input parameters

- [`foursail_t_o()`](foursail_t_o.md) : Performs PROSAIL simulation
  based on a set of combinations of input parameters

- [`foursail_t_s()`](foursail_t_s.md) : Performs PROSAIL simulation
  based on a set of combinations of input parameters

- [`gauss_byMin_Max()`](gauss_byMin_Max.md) : Gaussion distribution

- [`get.Altitude2Pa()`](get.Altitude2Pa.md) : Get atmospheric pressure
  (in hPa) as a function of altitude

- [`get.cars.pls()`](get.cars.pls.md) : Perform CARS-PLS analysis.

- [`get.coef.SMAC()`](get.coef.SMAC.md) : Get the coefficients for the
  SMAC model

- [`get.Extraterrestrial_radiance()`](get.Extraterrestrial_radiance.md)
  : get.Extraterrestrial_radiance

- [`get.indices.v2()`](get.indices.v2.md) : Compute vegetation indices
  from reflectance spectra (v2, expanded index set)

- [`get.inversion()`](get.inversion.md) : get.inversion of the plant
  traits using different machine learning models

- [`get.inversionOpt()`](get.inversionOpt.md) : LUT Inversion Using a
  Radiative Transfer Model (RTM)

- [`get.LUTfromRanges()`](get.LUTfromRanges.md) : Get a LUT based on a
  table with Min and Max ranges

- [`get.marmit.rsoil()`](get.marmit.rsoil.md) : Build a
  canopy-model-ready soil reflectance spectrum from MARMIT

- [`get.marmit1()`](get.marmit1.md) : Calculate Spectral Reflectance for
  Wet Soils Using MARMIT-1

- [`get.marmit2()`](get.marmit2.md) : Simulation of Spectral Reflectance
  of Wet Soils in the Solar Domain using MARMIT-2

- [`get.packages()`](get.packages.md) : Get the packages

- [`get.plot.cars.pls()`](get.plot.cars.pls.md) : Generate plot for
  CARSPLS analysis.

- [`get.plot.ML()`](get.plot.ML.md) : This function plots predictions
  from a machine learning (ML) model on both training and testing
  datasets.

- [`get.plot.plsr()`](get.plot.plsr.md) : Plot Predictions from PLSR
  Model

- [`get.plots()`](get.plots.md) : Get a plot with simulations

- [`get.RTM()`](get.RTM.md) : get.RTM - Run Radiative Transfer Model
  (RTM)

- [`get.satellite_collection()`](get.satellite_collection.md) : Retrieve
  Satellite Collections

- [`get.sentinel2_cube()`](get.sentinel2_cube.md) : Create and Process a
  Sentinel-2 Data Cube

- [`get.smac()`](get.smac.md) : Apply an atmospheric correction using
  SMAC method (Rahman and Dedieu, 1994)

- [`get.sobol.indices()`](get.sobol.indices.md) : Get first and total
  Sobol indices by Carlos Camino

- [`get.spectra.convolved()`](get.spectra.convolved.md) : Get Convolved
  Spectra

- [`get.spectra.spart()`](get.spectra.spart.md) : Get Spectral
  Characteristics for the SPART Model

- [`get.spectral.convolution.gaussian()`](get.spectral.convolution.gaussian.md)
  : Convolve reflectance onto a sensor using only NOMINAL band
  characteristics (center wavelength + FWHM, or center + band edges) –
  for sensors with no real measured per-nm SRF curve available at all,
  approximated as a Gaussian response (optionally truncated to a
  published band edge range).

- [`get.spectral.convolution()`](get.spectral.convolution.md) :
  get.spectral.convolution

- [`get.spectral.convolution.rfl()`](get.spectral.convolution.rfl.md) :
  Get Spectral Convolution for Reflectance

- [`get.spectral.convolution.srf()`](get.spectral.convolution.srf.md) :
  Convolve reflectance onto a sensor using a plain per-band SRF table

- [`get.spectral.derivative()`](get.spectral.derivative.md) : Calculate
  spectral derivative

- [`get.spectral()`](get.spectral.md) : Get spectral bandset for
  interpolation

- [`get.spectral.sensitivity()`](get.spectral.sensitivity.md) : Spectral
  global sensitivity analysis (Sobol total index per wavelength)

- [`get.srf.from_fwhm()`](get.srf.from_fwhm.md) : Generate Spectral
  Response Function from Full Width at Half Maximum (FWHM)

- [`get.stats.ind()`](get.stats.ind.md) : Get Statistical Scores between
  measurd and predicted single variable

- [`get.stats.plsr()`](get.stats.plsr.md) : Get Statistical Scores for
  PLSR Model

- [`get.stats()`](get.stats.md) : Get Statistical Scores for ML Models

- [`getBSM.toolsRTM()`](getBSM.toolsRTM.md) : Brightness-Shape-Moisture
  soil model

- [`getCor()`](getCor.md) : Generate multiple mutually-correlated
  variables at a target correlation level

- [`getCWSI()`](getCWSI.md) : Get CWSI for almonds

- [`getFLD2()`](getFLD2.md) : Get FLD2

- [`getFLD2_SE()`](getFLD2_SE.md) : Get FLD2

- [`getFluspect.B()`](getFluspect.B.md) : Leaf FLUSPECT-B model

- [`getFluspect.Cx()`](getFluspect.Cx.md) : Leaf FLUSPECT-B-Cx model

- [`getIndices()`](getIndices.md) : GetIndices: A Function to Estimate
  Common Spectral Indices

- [`getIndicesSE2.ML()`](getIndicesSE2.ML.md) : extract spectral indices
  at Sentinel-2 resolution a sort number of indices

- [`getIndicesSE2()`](getIndicesSE2.md) : extract spectral indices at
  Sentinel-2 resolution

- [`getKmeans()`](getKmeans.md) : K-means Classification on a
  RasterStack This function performs a k-means unsupervised
  classification of a stack of rasters. Number of clusters can be
  specified, as well as number of iterations and starting sets. An
  optional geographic weighting system can be turned on that constrains
  clusters to a geographic area, by including coordinates in the
  clustering. All variables are normalized before clustering is
  performed.
  https://rdrr.io/github/ozjimbob/ecbtools/man/raster.kmeans.html

- [`getLUT()`](getLUT.md) : Get LUT for radiative transfer models

- [`getLUTs()`](getLUTs.md) : Get LUTs for radiative transfer models

- [`getLUT_liberty()`](getLUT_liberty.md) : Get LUT for radiative
  transfer models

- [`getMLmodel()`](getMLmodel.md) : getMLmodel with a prefixed
  configuration

- [`getMLmodel.withRetrain()`](getMLmodel.withRetrain.md) : getMLmodel
  is a function for retrain a deep model with a prefixed configuration

- [`GetMosaics()`](GetMosaics.md) : Get a Mosaic from TIFFs Tiles from
  (Forest type 2018 later from COPERNICUS)

- [`getPredicts()`](getPredicts.md) : getPredicts

- [`getReverse.trans()`](getReverse.trans.md) : Reverse a caret
  preProcess transformation and round the result

- [`getSeries()`](getSeries.md) : get series in a dataframe from rasters

- [`getSim_fromLUT()`](getSim_fromLUT.md) : Simulate reflectance spectra
  while sweeping one trait, all others fixed

- [`getSpatialTrait()`](getSpatialTrait.md) : function for Spatial
  mapping of the main traits

- [`getSpatial_index()`](getSpatial_index.md) : this function produces
  Spatial maps of the spectral index

- [`getSpectraIndices()`](getSpectraIndices.md) : this function produces
  Spatial maps of the spectral indices

- [`GetSpectralseries()`](GetSpectralseries.md) : Extract all bands from
  SE images (adapted to NETCDF dataset)

- [`getSplitData()`](getSplitData.md) : getSplitData for ML models

- [`getSplitData_noMessages()`](getSplitData_noMessages.md) :
  getSplitData for ML models with no messages

- [`getStacks()`](getStacks.md) : Get TIFF from a folder and generate
  the stack.

- [`getTIFFs()`](getTIFFs.md) : Extract all bands from SE images
  (adapted to NETCDF dataset)

- [`getVIF()`](getVIF.md) : Calculate Variance Inflation Factor (VIF)

- [`get_append()`](get_append.md) : Append tables

- [`get_bounding_box()`](get_bounding_box.md) : Define a Bounding Box
  Around a Scenario Centroid

- [`get_data()`](get_data.md) : get random plant traits using a uniform
  distribution

- [`get_distributionLUT()`](get_distributionLUT.md) : Build a LUT with a
  per-trait distribution choice (Uniform or Gaussian) and an optional
  Car~Cab correlation

- [`get_response.R()`](get_response.R.md) : spectral response function

- [`get_simulations()`](get_simulations.md) : Simulate spectra with RT
  models

- [`hybrid_inversion()`](hybrid_inversion.md) : Inversion of plant
  traits using ML models

- [`hybrid_inversionE()`](hybrid_inversionE.md) : Inversion of plant
  traits using ML models (Ensemble variant)

- [`inform()`](inform.md) : The INvertible FOrest Reflectance Model
  coupled with leaf models

- [`Jfunc1()`](Jfunc1.md) : edit 2017 12 28: change sampling of angles
  to match with Jfunc1.m

- [`jfunc1_()`](jfunc1_.md) : Title

- [`Jfunc2()`](Jfunc2.md) : J2 function with avoidance of singularity
  problem edit 2017 12 28: change sampling of angles to match with
  Jfunc2.m

- [`Jfunc3()`](Jfunc3.md) : J3 function with avoidance of singularity
  problem edit 2017 12 28: change sampling of angles to match with
  Jfunc3.m

- [`Jfunc4()`](Jfunc4.md) : J4 function for treating (near) conservative
  scattering edit 2021 11 28: change sampling of angles to match with
  Jfunc4

- [`liberty()`](liberty.md) : LIBERTY model

- [`MAE()`](MAE.md) : Function that returns Mean Absolute Error

- [`NonConservativeScattering()`](NonConservativeScattering.md) :
  computes non conservative scattering conditions

- [`normalize()`](normalize.md) : Normalize

- [`numjacobian()`](numjacobian.md) : num-jacobian function

- [`plotspectra()`](plotspectra.md) : Plot one or more spectra from a
  data frame

- [`plotspectra_image()`](plotspectra_image.md) : Plot the spectra in
  points over the image

- [`plotspectra_SE()`](plotspectra_SE.md) : Plot spectra as discrete
  points at Sentinel band positions

- [`prospect_DB()`](prospect_DB.md) : PROSPECT-Dynamic with brown
  pigments

- [`prospect_PRO()`](prospect_PRO.md) : PROSPECT-PRO

- [`rbinomcor()`](rbinomcor.md) : Generate a vector from a binomial
  distribution correlated with another distribution

- [`rchisqcor()`](rchisqcor.md) : Generate a vector from a chi-square
  distribution correlated with another distribution

- [`resample_fun()`](resample_fun.md) : Generate a normalized Gaussian
  spectral response function

- [`rgammacor()`](rgammacor.md) : Generate a vector from a Gamma
  distribution correlated with another distribution

- [`rlnormcor()`](rlnormcor.md) : Generate a single variable from a
  distribution correlated with another distribution

- [`RMSE()`](RMSE.md) : Function that returns Root Mean Square Error

- [`rnegbinomcor()`](rnegbinomcor.md) : Generate a vector from a
  negative binomial distribution correlated with another distribution

- [`rnormcor()`](rnormcor.md) : Generate a single variable from a
  distribution correlated with another distribution

- [`rnormcorV()`](rnormcorV.md) : Generate a vector from a normal
  distribution correlated with another distribution

- [`rpoiscor()`](rpoiscor.md) : Generate a vector from a Poisson
  distribution correlated with another distribution

- [`runifcor.cor()`](runifcor.cor.md) : Generate a vector from a uniform
  distribution correlated with another distribution

- [`run_simulation()`](run_simulation.md) : Run a vegetation
  reflectance/energy-balance simulation, choosing models interactively

- [`rweibullcor()`](rweibullcor.md) : Generate a vector from a Weibull
  distribution correlated with another distribution

- [`sigmoid.soil()`](sigmoid.soil.md) : Calculate Soil Moisture Content
  Using a Sigmoid Model

- [`simulate_RTM()`](simulate_RTM.md) : Simulate canopy reflectance with
  any supported leaf model + any supported canopy model

- [`Sint()`](Sint.md) : Simpson integration

- [`soilwat()`](soilwat.md) :

  soilwat function `soilwat` In this model it is assumed that the water
  film area is built up

- [`SPART()`](SPART.md) : Compute Top-of-Canopy and Top-of-Atmosphere
  Reflectance and Radiance

- [`SPART.simN()`](SPART.simN.md) : Compute Top-of-Canopy and
  Top-of-Atmosphere Reflectance and Radiance with Simulations

- [`volscatt()`](volscatt.md) : Compute volume scattering functions and
  interception coefficients for given solar zenith, viewing zenith,
  azimuth and leaf inclination angle.
