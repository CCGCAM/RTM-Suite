% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggreg.R
\name{aggreg}
\alias{aggreg}
\title{Aggregates MODTRAN data over SCOPE bands by averaging}
\usage{
aggreg(atmfile, SCOPEspec)
}
\arguments{
\item{atmfile}{character. Path to a MODTRAN \code{.atm} text file with a header row, wavelength in column 2, and the relevant transmittance/radiance functions in columns 3-20.}

\item{SCOPEspec}{list. SCOPE spectral band definition, with elements \code{nreg} (number of spectral regions), \code{start} (start wavelength of each region, nm), \code{end} (end wavelength of each region, nm), and \code{res} (spectral resolution of each region, nm).}
}
\value{
numeric matrix with \code{nwS} rows (total number of SCOPE bands across all regions) and 6 columns. The six columns correspond to: (1) \code{Eso*cos(tts)/pi}, (2) \code{rdd}, (3) \code{tss}, (4) \code{tsd}, (5) \code{tssrdd}, (6) \code{La} (path radiance).
}
\description{
\code{aggreg} reads a MODTRAN \code{.atm} output file and averages six
relevant atmospheric radiative transfer quantities (extraterrestrial solar
irradiance, hemispherical reflectance, direct and diffuse transmittances,
and path radiance) onto the (possibly coarser, multi-resolution) SCOPE
spectral band definition.
}
\examples{
\dontrun{
M <- aggreg(atmfile, SCOPEspec)
}
}
\author{
Carlos Camino
}
