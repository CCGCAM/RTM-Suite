% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RTMo_functions.R
\name{get.fluxprofile}
\alias{get.fluxprofile}
\title{get.fluxprofile}
\usage{
get.fluxprofile(
  Esun_,
  Esky_,
  rsoil,
  Xss,
  Xsd,
  Xdd,
  R_sd,
  R_dd,
  nl,
  nwl,
  rs.thermal = 0.06
)
}
\arguments{
\item{Esun_}{numeric vector of length \code{nwl}. Top-of-canopy direct solar irradiance spectrum.}

\item{Esky_}{numeric vector of length \code{nwl}. Top-of-canopy diffuse sky irradiance spectrum.}

\item{rsoil}{numeric vector of length \code{nwl}. Soil reflectance spectrum (bottom boundary condition).}

\item{Xss}{numeric vector of length \code{nl}. Direct-direct transmittance of each layer, as returned by \code{\link{get.reflectances}}.}

\item{Xsd}{numeric matrix with \code{nl} rows and \code{nwl} columns. Normalized direct-to-diffuse transmittance of each layer, as returned by \code{\link{get.reflectances}}.}

\item{Xdd}{numeric matrix with \code{nl} rows and \code{nwl} columns. Normalized diffuse-to-diffuse transmittance of each layer, as returned by \code{\link{get.reflectances}}.}

\item{R_sd}{numeric matrix with \code{nl+1} rows and \code{nwl} columns. Directional-hemispherical reflectance at the top of each layer, as returned by \code{\link{get.reflectances}}.}

\item{R_dd}{numeric matrix with \code{nl+1} rows and \code{nwl} columns. Hemispherical-hemispherical reflectance at the top of each layer, as returned by \code{\link{get.reflectances}}.}

\item{nl}{integer. Number of canopy layers.}

\item{nwl}{integer. Number of wavelengths in the spectral domain.}

\item{rs.thermal}{numeric. Soil reflectance used to initialize the flux matrices and (when \code{Xsd} does not cover the thermal region) to extend the spectral domain into the thermal region; default 0.06.}
}
\value{
A list with:
\describe{
\item{Es_}{numeric matrix with \code{nl+1} rows and \code{nwl} columns. Direct solar irradiance at the top of each layer (down to the soil).}
\item{Emin_}{numeric matrix with \code{nl+1} rows and \code{nwl} columns. Downward diffuse irradiance at the top of each layer.}
\item{Eplu_}{numeric matrix with \code{nl+1} rows and \code{nwl} columns. Upward diffuse irradiance at the top of each layer.}
}
}
\description{
\code{get.fluxprofile} propagates the top-of-canopy direct and diffuse
irradiance down through the \code{nl} canopy layers (and reflects them back
up), using the layer transmittance/reflectance factors from
\code{\link{get.reflectances}}, to obtain the vertical profile of direct
and diffuse radiation fluxes within the canopy.
}
\examples{
\dontrun{
Eflux <- get.fluxprofile(Esun_, Esky_, rsoil, Xss, Xsd, Xdd, R_sd, R_dd, nl, nwl)
}
}
\author{
\if{html}{\out{<div class="sourceCode">}}\preformatted{ Wout Verhoef, Christiaan van der Tol, Joris Timmermans (Original version in Matlab)
}\if{html}{\out{</div>}}

Carlos Camino (Ported version into R)
}
