% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebal_functions.R
\name{get.aggregator.ebal}
\alias{get.aggregator.ebal}
\title{get.aggregator.ebal}
\usage{
get.aggregator.ebal(
  LAI,
  sunlit_flux,
  shaded_flux,
  Fs,
  data.canopy,
  canopy.choice
)
}
\arguments{
\item{LAI}{numeric. Total (one-sided) leaf area index of the canopy (m2 m-2).}

\item{sunlit_flux}{array with dimensions (nlincl, nlazi, nlayers) (or as required by \code{canopy.choice}). Leaf-scale flux for sunlit leaves.}

\item{shaded_flux}{numeric vector of length nlayers. Leaf-scale flux for shaded leaves (averaged over leaf angle, one value per layer).}

\item{Fs}{numeric vector of length nlayers. Fraction of sunlit leaf area at each canopy layer (\code{Ps}); the shaded contribution is weighted by \code{1 - Fs}.}

\item{data.canopy}{list. Canopy structural properties: \code{nlayers} (number of canopy layers), \code{nlincl} (number of leaf inclination classes), \code{nlazi} (number of leaf azimuth classes), \code{lidf} (leaf inclination distribution function weights).}

\item{canopy.choice}{character. Aggregation mode passed to \code{\link{meanleaf.v2}}: one of \code{'angles'}, \code{'layers'}, or \code{'angles_and_layers'}.}
}
\value{
numeric. LAI-scaled canopy-integrated total flux \code{flux_tot}, combining the sunlit and shaded contributions.
}
\description{
\code{get.aggregator.ebal} aggregates a leaf-scale flux, computed
separately for sunlit and shaded leaves, into a single LAI-scaled
canopy-integrated total flux, by internally calling
\code{\link{meanleaf.v2}} for each contribution and combining them
weighted by the fraction of sunlit leaf area \code{Fs}.
}
\examples{
\dontrun{
flux_tot <- get.aggregator.ebal(LAI, sunlit_flux, shaded_flux, Fs,
                                 data.canopy, canopy.choice = 'angles_and_layers')
}
}
\author{
\if{html}{\out{<div class="sourceCode">}}\preformatted{Christiaan van der Tol (Original version in Matlab)
}\if{html}{\out{</div>}}

Carlos Camino (Ported version into R)
}
