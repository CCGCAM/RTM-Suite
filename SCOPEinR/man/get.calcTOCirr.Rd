% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RTMo_functions.R
\name{get.calcTOCirr}
\alias{get.calcTOCirr}
\title{get.calcTOCirr}
\usage{
get.calcTOCirr(atmo, meteo, rdd, rsd, wl, nwl, spectral, soil)
}
\arguments{
\item{atmo}{list or data.frame. Atmospheric input, either pre-computed \code{Esun_}/\code{Esky_} spectra, or MODTRAN output containing wavenumber (\code{WN}) and transmittance/radiance functions \code{T1, T2, T3, T4, T5, T12, T16}.}

\item{meteo}{list. Meteorological data; must contain \code{Ta} (air temperature, deg C) and, when rescaling is required, \code{Rin} (incoming shortwave radiation, W m-2) and \code{Rli} (incoming longwave radiation, W m-2). Use \code{-999} for \code{Rin} to skip rescaling.}

\item{rdd}{numeric vector of length \code{nwl}. Top-of-canopy hemispherical-hemispherical reflectance spectrum.}

\item{rsd}{numeric vector of length \code{nwl}. Top-of-canopy directional-hemispherical reflectance spectrum.}

\item{wl}{numeric vector of length \code{nwl}. Wavelengths of the spectral domain (nm).}

\item{nwl}{integer. Number of wavelengths in the spectral domain.}

\item{spectral}{list. Spectral-domain object (as built elsewhere in this
package, e.g. \code{data.spectral} in \code{getRTMo()}) -- used here
for its \code{reg1}/\code{reg2}/\code{reg3}/\code{IwlT} index ranges.}

\item{soil}{list. Soil properties object (e.g. \code{data.soil} in
\code{getRTMo()}) -- used here for its \code{rs_thermal} value.}
}
\value{
A list with:
\describe{
\item{Esun_}{numeric vector of length \code{nwl}. Top-of-canopy direct solar irradiance spectrum.}
\item{Esky_}{numeric vector of length \code{nwl}. Top-of-canopy diffuse sky irradiance spectrum.}
}
}
\description{
\code{get.calcTOCirr} calculates the top-of-canopy (TOC) direct solar
(\code{Esun_}) and diffuse sky (\code{Esky_}) irradiance spectra. If
\code{atmo} already contains pre-computed \code{Esun_}/\code{Esky_} these
are returned directly; otherwise they are derived from the MODTRAN
atmospheric transmittance/radiance functions (T1-T16) evaluated at the
SCOPE wavelengths, optionally rescaled to match the observed broadband
incoming shortwave (\code{Rin}) and longwave (\code{Rli}) radiation.
}
\examples{
\dontrun{
TOCirr <- get.calcTOCirr(atmo, meteo, rdd, rsd, wl, nwl, spectral, soil)
}
}
\author{
\if{html}{\out{<div class="sourceCode">}}\preformatted{ Wout Verhoef, Christiaan van der Tol, Joris Timmermans (Original version in Matlab)
}\if{html}{\out{</div>}}

Carlos Camino (Ported version into R)
}
