% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ebal_functions.R
\name{meanleaf.v2}
\alias{meanleaf.v2}
\title{meanleaf.v2}
\usage{
meanleaf.v2(data.canopy, F_, canopy.choice, Ps)
}
\arguments{
\item{data.canopy}{list. Canopy structural properties: \code{nlayers} (number of canopy layers), \code{nlincl} (number of leaf inclination classes), \code{nlazi} (number of leaf azimuth classes), \code{lidf} (leaf inclination distribution function weights).}

\item{F_}{array or numeric vector. Leaf-scale flux/property to average; expected as an array with dimensions (nlincl, nlazi, nlayers) for \code{canopy.choice = 'angles'} or \code{'angles_and_layers'}, or as a numeric vector of length \code{nlayers} for \code{canopy.choice = 'layers'}.}

\item{canopy.choice}{character. Averaging mode: \code{'angles'} (average over leaf inclination/azimuth only), \code{'layers'} (weighted average over layers using \code{Ps}), or \code{'angles_and_layers'} (average over both).}

\item{Ps}{numeric vector of length nlayers, or scalar. Layer weight (e.g. fraction of sunlit leaf area) used when \code{canopy.choice} includes \code{'layers'}.}
}
\value{
numeric. Single averaged value \code{Fout_vertical}.
}
\description{
\code{meanleaf.v2} averages a leaf-scale flux or property over leaf angle
classes and/or canopy layers, weighted by the leaf inclination
distribution function (\code{lidf}) and/or a layer weight \code{Ps}
(typically the sunlit/shaded fraction). This is a vectorized variant of
\code{\link{meanleaf}} used internally by \code{\link{get.aggregator.ebal}}.
}
\examples{
\dontrun{
Fout <- meanleaf.v2(data.canopy, F_, canopy.choice = 'layers', Ps)
}
}
\author{
\if{html}{\out{<div class="sourceCode">}}\preformatted{Christiaan van der Tol (Original version in Matlab)
}\if{html}{\out{</div>}}

Carlos Camino (Ported version into R)
}
