% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getinputLUT.R
\name{getinputLUT}
\alias{getinputLUT}
\title{Get main input for SCOPE}
\usage{
getinputLUT(inputLUT, dataset, calc.heat, calc.rss_rbs)
}
\arguments{
\item{inputLUT}{data.frame or matrix. LUT/input table with one column per named parameter (e.g. \code{N}, \code{Cab}, \code{LAI}, \code{tts}, \code{Ta}, ...) and one row per simulation.}

\item{dataset}{character. Which group of parameters to extract; one of \code{'leafbio'}, \code{'meteo'}, \code{'canopy'}, \code{'angles'}, \code{'soil'}, \code{'Coordinates'}, \code{'mly'} or \code{'timeseries'}.}

\item{calc.heat}{numeric. Soil heat flux method used only when \code{dataset == 'soil'}: \code{0} estimates \code{GAM} with \code{\link{Soil_Inertia0}} (from \code{cs}, \code{rhos}, \code{lambdas}), \code{1} estimates \code{GAM} with \code{\link{Soil_Inertia1}} (from \code{SMC}), any other value (default \code{2}) leaves \code{GAM} as \code{NA} (soil heat flux computed elsewhere as \code{0.35*Rn}).}

\item{calc.rss_rbs}{numeric. Flag used only when \code{dataset == 'soil'}: \code{0} (default) uses \code{rss}/\code{rbs} directly from \code{inputLUT}; any other value recomputes them from \code{SMC}/\code{LAI}/\code{rbs} via \code{\link{calc_rssrbs}}.}
}
\value{
A named list with the extracted parameters for the requested \code{dataset} (e.g. \code{leafbio}, \code{meteo}, \code{canopy}, \code{angles}, \code{soil}, \code{coord}, \code{timeseries}, or \code{mly}).
}
\description{
\code{getinputLUT} extracts one group of SCOPE model input parameters
(a single row, or a set of rows, of a LUT/input table) into the named
list structure expected by the rest of SCOPEinR (leaf biology, meteo,
canopy structure, geometry, soil, coordinates, time series, or multilayer
leaf biochemistry profile). For \code{dataset == 'soil'}, it also derives
the soil thermal inertia (\code{GAM}, via \code{\link{Soil_Inertia0}} or
\code{\link{Soil_Inertia1}}) and, optionally, the soil/canopy resistances
\code{rss}/\code{rbs} (via \code{\link{calc_rssrbs}}).
}
\examples{
\dontrun{
leafbio <- getinputLUT(inputLUT, dataset = 'leafbio')
soil <- getinputLUT(inputLUT, dataset = 'soil', calc.heat = 2, calc.rss_rbs = 0)
}
}
\author{
Carlos Camino
}
