% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getCSV.R
\name{getCSV}
\alias{getCSV}
\title{Get CSV Data from Multiple Folders}
\usage{
getCSV(path.out = NULL, n.folders = 4, files.names = "All")
}
\arguments{
\item{path.out}{The path to the root folder containing the subfolders with CSV files.}

\item{n.folders}{The number of folders from the end of the directory structure to consider.}

\item{files.names}{The type of files to look for. Options: "Fluorescence", "Reflectance", or "Radiance".}
}
\value{
A list with \code{data} (list, one element per file pattern in \code{files.names}, each the row-bound data across the last \code{n.folders} folders), \code{names} (the file patterns with the \code{.csv} suffix stripped), and \code{lut} (the row-bound \code{Parameters/inputLUT.csv} across those same folders).
}
\description{
This function reads CSV files from multiple folders and combines them into a list.
}
\examples{
\dontrun{
path.out <- "outs"
n.folders <- 4
files.names <- "Fluorescence"
combined_data <- getCSV(path.out, n.folders, files.names)
}
}
