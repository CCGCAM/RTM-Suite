% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getIndices.R
\name{getIndices}
\alias{getIndices}
\title{GetIndices: A Function to Estimate Common Spectral Indices}
\usage{
getIndices(data, pattern.rfl = "R.", factor = NULL, spectral.domain = NULL)
}
\arguments{
\item{data}{A dataframe or matrix containing reflectance values in named columns.}

\item{pattern.rfl}{A character string indicating the prefix pattern for reflectance column names.
Default is \code{'R.'}, which assumes column names like \code{'R.400'}, \code{'R.670'}, etc.}

\item{factor}{A numeric scaling factor applied to reflectance values (e.g., 1/10000 for scaling
reflectance from digital numbers). Default is \code{NULL}, meaning no scaling is applied (factor = 1).}

\item{spectral.domain}{A character string specifying the spectral domain for index calculation.
Available options are:
\itemize{
\item \code{"VNIR"} (default): Includes wavelengths between 400-850 nm.
\item \code{"SWIR"}: Includes wavelengths between 800-1750 nm.
\item \code{"VNIR-SWIR"}: Includes wavelengths between 400-1750 nm.
If \code{NULL}, it defaults to \code{"VNIR"}.
}}
}
\value{
A dataframe containing the original data along with computed spectral indices.
}
\description{
This function calculates common spectral vegetation indices from reflectance data,
using user-specified spectral domains and a scaling factor if needed.
}
\examples{
# Example 1: Compute spectral indices using default VNIR domain
data_example <- data.frame(R.400 = runif(10, 0, 1), R.670 = runif(10, 0, 1), R.800 = runif(10, 0, 1))
result <- getIndices(data_example)

# Example 2: Compute indices using SWIR domain with a scaling factor of 1/10000
data_example <- data.frame(R.850 = runif(10, 0, 1), R.1200 = runif(10, 0, 1), R.1600 = runif(10, 0, 1))
result <- getIndices(data_example, spectral.domain = "SWIR", factor = 1/10000)

# Example 3: Compute indices for VNIR-SWIR domain with custom reflectance column pattern
data_example <- data.frame(Reflectance_450 = runif(10, 0, 1), Reflectance_900 = runif(10, 0, 1))
result <- getIndices(data_example, pattern.rfl = "Reflectance_", spectral.domain = "VNIR-SWIR")

}
