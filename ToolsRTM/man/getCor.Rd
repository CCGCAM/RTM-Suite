% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getCor.R
\name{getCor}
\alias{getCor}
\title{Generate multiple mutually-correlated variables at a target correlation level}
\usage{
getCor(
  n_inputs = NULL,
  nLUT = 100,
  distribution = "Uniform",
  setseed = 123,
  rho = NULL,
  Varnames = NULL,
  MinRange = NULL,
  MaxRange = NULL
)
}
\arguments{
\item{n_inputs}{integer. How many variables to generate.}

\item{nLUT}{integer. How many values per variable (LUT rows). Default 100.}

\item{distribution}{character. \code{"Uniform"} or \code{"Normal"}. Default \code{"Uniform"}.}

\item{setseed}{integer. Random seed. Default 123.}

\item{rho}{numeric (-1 to 1). Target pairwise correlation between every pair of variables.}

\item{Varnames}{character vector, length \code{n_inputs}. Column names for the output;
if \code{NULL}, columns are named \code{Var_1}, \code{Var_2}, ...}

\item{MinRange}{numeric vector, length \code{n_inputs}. Minimum of each variable's output range.}

\item{MaxRange}{numeric vector, length \code{n_inputs}. Maximum of each variable's output range.}
}
\value{
A list: \code{LUT} (data.frame, \code{nLUT} rows x \code{n_inputs} columns) and
\code{Covarianza} (the realized correlation matrix of \code{LUT}, for checking how close
the sample came to the requested \code{rho}).
}
\description{
Builds \code{n_inputs} variables, each \code{nLUT} values long, all
pairwise-correlated at (approximately) \code{rho}, then rescales each to
its own \code{[MinRange, MaxRange]} bound. Useful for LUTs where several
traits should co-vary (e.g. LAI and canopy height) rather than being
drawn independently.
}
\examples{
out <- getCor(n_inputs = 2, nLUT = 200, distribution = "Uniform", rho = 0.7,
               Varnames = c("LAI", "Height"), MinRange = c(0.5, 2), MaxRange = c(7, 30))
cor(out$LUT$LAI, out$LUT$Height)  # close to 0.7
}
