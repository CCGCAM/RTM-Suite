% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spectral_comvolution_rfl.R
\name{get.spectral.convolution.rfl}
\alias{get.spectral.convolution.rfl}
\title{Get Spectral Convolution for Reflectance}
\usage{
get.spectral.convolution.rfl(df, sensor.i, get.plots = T)
}
\arguments{
\item{df}{A data frame containing the high-resolution reflectance data to be convoluted.
Must have a \code{wave} column (wavelength, nm) and an \code{rfl} column (reflectance) -- e.g.
\code{data.frame(wave = dataSpec_PDB[, 1], rfl = my_reflectance)}.}

\item{sensor.i}{The sensor to convolve onto -- one of the package's bundled sensor objects,
e.g. \code{ToolsRTM::Sentinel2A.MSI}, \code{ToolsRTM::LANDSAT8.OLI}, \code{ToolsRTM::TerraAqua.MODIS}
(also accepts the bare sensor name as a character string, e.g. \code{"LANDSAT8.OLI"}, which is
resolved to the matching bundled object automatically). Supported sensors:
"LANDSAT4.TM", "LANDSAT5.TM", "LANDSAT7.ETM", "LANDSAT8.OLI", "Sentinel2A.MSI",
"Sentinel2B.MSI", "Sentinel3A.OLCI", "Sentinel3B.OLCI", and "TerraAqua.MODIS".}

\item{get.plots}{A boolean value indicating whether to generate plots of the spectral convolution.
Default is TRUE.}
}
\value{
A data frame containing the convoluted reflectance values for the specified sensor,
with columns \code{wave} (band center wavelength, nm) and \code{RFL} (convolved reflectance).
}
\description{
This function performs spectral convolution on reflectance data using a specified sensor's
spectral response function. The convolution is essential for simulating how well the sensor
captures the true reflectance values based on its spectral characteristics.
}
\examples{
df <- data.frame(wave = ToolsRTM::dataSpec_PDB[, 1],
                  rfl = 0.05 + 0.3 * ToolsRTM::dataSpec_PDB[, 1] / 2500) # toy reflectance
convoluted_results <- get.spectral.convolution.rfl(df, sensor.i = "LANDSAT8.OLI", get.plots = FALSE)
}
