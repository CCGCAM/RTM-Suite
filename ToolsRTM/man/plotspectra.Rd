% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getSim_fromLUT.R
\name{plotspectra}
\alias{plotspectra}
\title{Plot one or more spectra from a data frame}
\usage{
plotspectra(
  df,
  x = 1,
  y = 2,
  cols = y,
  xlim = range(df[, x], na.rm = T),
  ylim = range(df[, y], na.rm = T),
  main = "",
  xlab = "",
  ylab = "",
  ...
)
}
\arguments{
\item{df}{data.frame. Table containing the spectra to plot, with wavelengths in one column and reflectance/radiance values in one or more other columns.}

\item{x}{integer or character. Column index or name in \code{df} to use as the x-axis (typically wavelength). Default 1.}

\item{y}{integer or character vector. Column index(es) or name(s) in \code{df} to plot as spectra on the y-axis. Default 2.}

\item{cols}{vector. Line colors, one per element of \code{y}. Defaults to \code{y} itself.}

\item{xlim}{numeric vector of length 2. X-axis plot limits. Defaults to the data range of column \code{x}.}

\item{ylim}{numeric vector of length 2. Y-axis plot limits. Defaults to the data range of columns \code{y}.}

\item{main}{character. Plot title.}

\item{xlab}{character. X-axis label.}

\item{ylab}{character. Y-axis label.}

\item{...}{additional graphical parameters passed to the underlying \code{plot()} call.}
}
\value{
plot
}
\description{
Plot one or more spectra from a data frame
}
