% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Correlated_value.R
\name{correlatedValue}
\alias{correlatedValue}
\title{Generate a variable correlated with an existing one}
\usage{
correlatedValue(x, r)
}
\arguments{
\item{x}{numeric vector. The variable to correlate against.}

\item{r}{numeric (-1 to 1). Target correlation coefficient between \code{x} and the result.}
}
\value{
A numeric vector the same length as \code{x}, correlated with it at approximately \code{r}.
}
\description{
Draws a new variable \code{y = r*x + noise}, with noise scaled so that
\code{cor(x, y)} is approximately \code{r}. Negative results are clipped
to 0 (this assumes \code{x}/\code{y} are non-negative quantities, e.g.
pigment concentrations -- see \code{\link{get_distributionLUT}}'s
\code{DepCab} option, which uses this to correlate Car with Cab).
}
\examples{
Cab <- runif(200, 10, 80)
Car <- correlatedValue(x = Cab / 4, r = 0.8)
cor(Cab, Car)  # close to 0.8
}
