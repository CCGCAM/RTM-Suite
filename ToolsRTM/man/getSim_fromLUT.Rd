% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getSim_fromLUT.R
\name{getSim_fromLUT}
\alias{getSim_fromLUT}
\title{Simulate reflectance spectra while sweeping one trait, all others fixed}
\usage{
getSim_fromLUT(
  trait = "Cab",
  nmin = 0,
  nmax = 100,
  Interval = 10,
  psoil = 0.5,
  model = "PROSAIL",
  method = "ggplot"
)
}
\arguments{
\item{trait}{character. Name of the trait to sweep (e.g. \code{"Cab"}, \code{"LAI"}). Default \code{"Cab"}.}

\item{nmin, nmax}{numeric. Minimum/maximum value of \code{trait}'s sweep range.}

\item{Interval}{numeric. Step size between successive values of \code{trait} in the sweep.}

\item{psoil}{numeric (0-1). Soil brightness factor (0 = wet soil, 1 = dry soil), mixed
from \code{ToolsRTM::dataSpec_PDB}'s dry/wet reference spectra.}

\item{model}{character. Canopy simulation to run: \code{"PROSAIL"}, \code{"INFORM"}, or \code{"PROSPECT"}.}

\item{method}{character. \code{"ggplot"} (default) or \code{"classical"} (base-R plotting) for the spectral plot.}
}
\value{
If \code{method = "ggplot"}: a list with \code{LUT} (the swept-trait LUT used) and
\code{Plot} (the ggplot object). If \code{method = "classical"}: just the LUT data.frame
(the plot is drawn as a side effect via base graphics, not returned).
}
\description{
Builds a LUT where every trait is held at its default except \code{trait},
which is swept from \code{nmin} to \code{nmax} in steps of \code{Interval},
runs the canopy simulation, and plots the resulting spectra.
}
\examples{
\dontrun{
out <- getSim_fromLUT(trait = "Cab", nmin = 10, nmax = 80, Interval = 10, model = "PROSAIL")
out$Plot
}
}
