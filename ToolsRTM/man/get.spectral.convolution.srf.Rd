% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.spectral.convolution.srf.R
\name{get.spectral.convolution.srf}
\alias{get.spectral.convolution.srf}
\title{Convolve reflectance onto a sensor using a plain per-band SRF table}
\usage{
get.spectral.convolution.srf(df, srf, fwhm = NULL, get.plots = FALSE)
}
\arguments{
\item{df}{A data frame with a \code{wave} column (wavelength, nm) and an \code{rfl}
column (reflectance) -- same convention as \code{get.spectral.convolution.rfl()}.}

\item{srf}{A plain per-band SRF table: its FIRST column is wavelength
(nm, any column name), every other column is one sensor band's SRF
weight at that wavelength (e.g. \code{ToolsRTM::srf.prisma},
\code{ToolsRTM::srf.sentinel2a}, \code{ToolsRTM::srf.sentinel2b}).}

\item{fwhm}{Optional data frame with a \code{fwhm} column, one row per band in
\code{srf} (same order), e.g. \code{ToolsRTM::fwhm.prisma} -- when supplied, its
(precisely interpolated) FWHM values are used instead of the coarser
half-max-crossing estimate this function would otherwise derive
directly from \code{srf}'s own sampled weight profile. Sentinel-2A/B have no
equivalent bundled FWHM table, so leave this \code{NULL} for them.}

\item{get.plots}{logical, plot the convolved spectrum? Default \code{FALSE}.}
}
\value{
A data frame with one row per SRF column: \code{band} (index), \code{wl}
(SRF-weighted mean center wavelength, nm), \code{fwhm} (full width at half
maximum, nm), \code{RFL} (convolved reflectance).
}
\description{
\code{get.spectral.convolution.rfl()} needs a sensor object with SMAC
atmospheric-correction coefficients bundled (see its own docs for the 9
supported sensors) -- fine for Landsat/OLCI/MODIS, but PRISMA has no SMAC
coefficients at all, and Sentinel-2A/B additionally ship a second, plain,
publisher-original spectral response function table alongside their SMAC
bundle. This function convolves onto any of those "just a per-band SRF
table, nothing else" sensors instead -- one shared implementation for
\code{ToolsRTM::srf.prisma}, \code{ToolsRTM::srf.sentinel2a}, and
\code{ToolsRTM::srf.sentinel2b} (generalizes what used to be two separate,
app-only helper functions, \code{convolve_prisma()}/\code{convolve_smac_sensor()},
in the AEO-Course PROSAIL Shiny app's own app.R).
}
\examples{
df <- data.frame(wave = ToolsRTM::dataSpec_PDB[, 1],
                  rfl = 0.05 + 0.3 * ToolsRTM::dataSpec_PDB[, 1] / 2500)
prisma_bands <- get.spectral.convolution.srf(df, ToolsRTM::srf.prisma,
                                              fwhm = ToolsRTM::fwhm.prisma)
s2a_bands <- get.spectral.convolution.srf(df, ToolsRTM::srf.sentinel2a)
}
