% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hybrid_inversion.R
\name{hybrid_inversion}
\alias{hybrid_inversion}
\title{Inversion of plant traits using ML models}
\usage{
hybrid_inversion(
  LUT = NULL,
  input = NULL,
  split = 0.8,
  setseed = NULL,
  method = NULL,
  collinearity = NULL,
  pattern = NULL,
  trans = NULL,
  Field.data = NULL,
  acron = NULL
)
}
\arguments{
\item{LUT}{Dataset with inputs and Bands}

\item{input}{variable to estimate}

\item{split}{ratio between 0 and 1 for splitting the dataset in training and testing}

\item{setseed}{set random number}

\item{method}{Machine learning approach for estimating each plant traits, the options are 'SVM', 'RF' and 'LDA'}

\item{collinearity}{collinearity-and-stepwise-vif-selection or CARS method implemmented, options='VIF' and 'CARS'}

\item{pattern}{Please indicate the number of bands with same pattern'B'.}

\item{trans}{Please indicate is want a logarithm transformation to y variable . Default is T}

\item{Field.data}{dataframe with observations}

\item{acron}{acronynm for the observation measure: e.g., Cab_obsrv, where acron='_observ' and Cab has same name as input}
}
\value{
A list: \code{model} (the fitted ML model), \code{Stats} (accuracy statistics
on the held-out test split), and \code{Plot} (a ggplot scatter of predicted vs.
measured/simulated values for \code{input}). If \code{Field.data} is supplied, the
returned \code{Plot}/\code{Stats} are computed against the field observations instead
of the LUT's own test split.
}
\description{
Inversion of plant traits using ML models
}
