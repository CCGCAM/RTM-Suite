% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.simulator.R
\name{get.simulator}
\alias{get.simulator}
\title{Launch an interactive RTM Shiny app}
\usage{
get.simulator(app = "PROSAIL")
}
\arguments{
\item{app}{A character string indicating which simulator to launch.
Acceptable values are:
- \code{"PROSAIL"}: Launch the PROSAIL simulator.
- \code{"PROSAIL-BRDF"}: Launch the PROSAIL-BRF simulator.
- \code{"MARMIT"}: Launch the MARMIT simulator.
- \code{"SPART"}: Launch the SPART simulator. This option needs the SCOPEinR package
- \code{"SCOPE"}: Launch the SCOPE simulator. This option needs the SCOPEinR package
- \code{"getLUT"}: Launch the configuration LUT app.
- \code{"RTMs"}: Launch the RTM simulator.
- \code{"STAC"}: Launch the STAC simulator.
- \code{"Inversion"}: Launch the Inversion module for retrieving plant traits.
- \code{"default"}: Launch the default simulator (general).}
}
\value{
Launches the Shiny app (invisibly returns \code{NULL}); called for its side effect.
}
\description{
Launches one of the suite's interactive Shiny applications, bundled under
this package's \code{inst/applications/} (not \code{ToolsRTM}'s -- the
apps and their UI/data dependencies live in this separate, GitLab-only
companion package, see \code{DESCRIPTION}).
}
\examples{
\dontrun{
get.simulator("PROSAIL")
get.simulator("SCOPE")
get.simulator("MARMIT")
get.simulator("getLUT")
get.simulator("STAC")
}
}
